class Solution(object):
    def reverseBits(self, n):
        r = 0

        for i in range(32):
            a = n & 1      # get last bit
            n >>= 1        # remove last bit
            r <<= 1        # make space in result
            r += a         # add extracted bit

        return r
